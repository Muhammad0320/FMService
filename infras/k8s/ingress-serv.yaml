apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-serv
spec:
  ingressClassName: nginx
  rules:
    - host: posts.com
      http:
        paths:
          - path: /posts
            pathType: Prefix
            backend:
              service:
                name: post-clusterip-serv
                port:
                  number: 4000
